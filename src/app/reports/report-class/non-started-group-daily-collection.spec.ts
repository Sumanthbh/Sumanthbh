import { NonStartedGroupDailyCollection } from './non-started-group-daily-collection';

describe('NonStartedGroupDailyCollection', () => {
  it('should create an instance', () => {
    expect(new NonStartedGroupDailyCollection()).toBeTruthy();
  });
});
