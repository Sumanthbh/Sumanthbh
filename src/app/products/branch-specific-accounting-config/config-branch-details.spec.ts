import { ConfigBranchDetails } from './config-branch-details';

describe('ConfigBranchDetails', () => {
  it('should create an instance', () => {
    expect(new ConfigBranchDetails()).toBeTruthy();
  });
});
